services:
  credit-risk-app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data:ro
      - ./artifacts:/app/artifacts
    restart: unless-stopped

